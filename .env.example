DATABASE_URL=postgres://postgres:postgres@localhost:5432/zctl
PORT=3000
HOST=0.0.0.0
# Generate a strong secret: openssl rand -hex 32
JWT_SECRET=dev-secret-change-in-production-1
JWT_EXPIRY_AGENT=24h
JWT_EXPIRY_OPERATOR=90d
# Restrict to your frontend origin in production, e.g. https://zctl.example.com
CORS_ORIGIN=*
