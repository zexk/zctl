services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: zctl
      POSTGRES_USER: zctl
      POSTGRES_PASSWORD: zctl
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
